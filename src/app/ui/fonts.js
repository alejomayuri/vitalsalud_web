import { Kanit, Katibeh, Inter } from 'next/font/google'

export const inter = Inter({ subsets: ["latin"] });

export const kanit = Kanit({
    weight: ['400', '600'],
    subsets: ['latin']
})

export const katibeh = Katibeh({ 
    weight: '400',
    subsets: ['latin']
})